# OptiVox Frontend

Vite + React app for the OptiVox public site and organization dashboard.

```powershell
npm install
npm run dev
npm run build
```

Set `VITE_API_BASE_URL` when the backend is not served from the same origin.
